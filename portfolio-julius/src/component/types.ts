export interface Skill {
    name: string;
  }
  
  export interface Project {
    name: string;
    description: string;
    link: string;
    languages?: string[];
    image: string;
    images?: string[];
  }